# Add unit tests for frontend components

## Summary
Adds unit tests for HomePage, CartPage, Categories, cart context, and useCategory hook. Also fixes duplicate key in `Prices.js`, adds React import to `cart.js`, and updates Jest config to include context and hooks tests.

**Test files:** HomePage.test.js, CartPage.test.js, Categories.test.js, cart.test.js, useCategory.test.js
